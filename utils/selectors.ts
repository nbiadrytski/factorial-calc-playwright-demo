export enum HomePageSelectors {
  enterIntegerInput = "Enter an integer",
  calculateButton = "css=#getFactorial",
  calculationResultText = "xpath=//p[@id='resultDiv']",
}

export enum PrivacyPageSelectors {
  privacyPageMainText = "This is the privacy document. We are not yet ready with it. Stay tuned!",
}

export enum TermsAndConditionsPageSelectors {
  termsAndConditionsPageMainText = "This is the terms and conditions document. We are not yet ready with it. Stay tuned!",
}
